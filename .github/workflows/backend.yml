name: 🚀 Deploy Frontend to Pages

on:
  push:
    branches: [main]
  # 用于手动触发
  workflow_dispatch:

# 设置权限
permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: ⬇️ Checkout Repository
        uses: actions/checkout@v4

      # 假设所有静态文件（index.html, backtest.html, ai.html 等）都在根目录
      # 或者在您截图中的 'docs' 目录下。
      # 您的 index.html 引用了 './backtest.html' 和 './ai.html'。
      # ⚠️ 注意：根据您的截图，'index.html' 在根目录，但 'backend.yml'
      # 上传的是 'docs' 目录。为了部署成功，我假设您会把所有 HTML 
      # 文件移动到 'docs/' 目录下。

      - name: 📂 Setup Pages
        uses: actions/configure-pages@v4

      - name: ⬆️ Upload Pages artifact (assuming static files are in 'docs')
        uses: actions/upload-pages-artifact@v3
        with:
          # 您的原文件上传了 docs 目录
          path: docs

  deploy:
    name: 部署到 GitHub Pages
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: 🚀 Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
